streamlit==1.24.1
pandas==1.5.3
openpyxl==3.1.2
smtplib  # This comes with Python standard library, so it doesn't need to be installed separately
email  # This comes with Python standard library, so it doesn't need to be installed separately
pywhatkit==5.4
xvfbwrapper==0.2.9
pyautogui==0.9.53
